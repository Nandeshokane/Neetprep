{
  "name": "neet-practice-fullstack",
  "version": "1.0.0",
  "description": "NEET Practice App",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "npm install --prefix backend && npm install --include=dev --prefix frontend && npm run build --prefix frontend",
    "start": "cd backend && node server.js"
  }
}
